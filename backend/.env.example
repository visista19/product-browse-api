# Postgres connection string (same Supabase URI used for seeding).
DATABASE_URL=postgresql://postgres.xxxx:[YOUR-PASSWORD]@aws-0-xx.pooler.supabase.com:5432/postgres?sslmode=require

# Port the API listens on. Render sets this automatically; default 3000 locally.
PORT=3000
